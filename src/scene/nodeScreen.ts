/** Position à l'écran (en pixels) de chaque bulle de conversation, mise à jour à chaque image par la scène 3D. */
export const nodeScreen = new Map<string, { x: number; y: number; size: number }>();
